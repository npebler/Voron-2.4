[gcode_macro START_FILTER]
gcode:
    SET_FAN_SPEED FAN=filter SPEED=100

[gcode_macro STOP_FILTER]
gcode:
    SET_FAN_SPEED FAN=filter SPEED=0

[gcode_macro FILTER_PRINTING]
gcode:
    SET_FAN_SPEED FAN=filter SPEED=80